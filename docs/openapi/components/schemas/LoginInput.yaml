type: object
required:
  - username
  - password
properties:
  username:
    description: netlink id of the user trying to sign in
    type: string
    example: admin
  password:
    description: the password of the user
    type: string
    example: password
    